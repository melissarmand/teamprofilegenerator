const Employee = require("../lib/Employee");

describe("Employee class", () => {
    it("Should include an email address", () => {

        
        expect().toBe();
    });
}); //these should go at the very end